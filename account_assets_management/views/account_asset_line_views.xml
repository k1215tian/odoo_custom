<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View account.asset.line View List -->
    <record id="view_account_asset_line_list" model="ir.ui.view">
        <field name="name">view.account.asset.line.list</field>
        <field name="model">account.asset.line</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- View account.asset.line form -->
    <record id="view_account_asset_line_form" model="ir.ui.view">
        <field name="name">view.account.asset.line.form</field>
        <field name="model">account.asset.line</field>
        <field name="arch" type="xml">
            <form string="Account Asset Line">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- View account.asset.line search -->
    <record id="view_account_asset_line_search" model="ir.ui.view">
        <field name="name">view.account.asset.line.search</field>
        <field name="model">account.asset.line</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action account.asset.line -->
    <record id="action_account_asset_line" model="ir.actions.act_window">
        <field name="name">Account Asset Line</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">account.asset.line</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Account Asset Line.
            </p>
        </field>
    </record>

</odoo>
